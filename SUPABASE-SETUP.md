# Инструкция по настройке автоматического удаления файлов в Supabase

## 📋 Что делает этот триггер?

Когда вы удаляете запись из таблицы `posters` в Supabase, триггер **автоматически удаляет** соответствующий файл изображения из Storage.

## 🚀 Как настроить (пошагово)

### Шаг 1: Откройте Supabase Dashboard

1. Перейдите на [supabase.com](https://supabase.com)
2. Войдите в свой аккаунт
3. Выберите ваш проект (random-dafont или как вы его назвали)

### Шаг 2: Откройте SQL Editor

1. В левом меню найдите **SQL Editor** (иконка с символом `</>`)
2. Нажмите на **SQL Editor**
3. Нажмите кнопку **"New query"** (или "+ New query")

### Шаг 3: Скопируйте и выполните SQL код

1. Откройте файл `supabase-trigger.sql` (он лежит рядом с этой инструкцией)
2. Скопируйте **весь** SQL код из файла
3. Вставьте код в окно SQL Editor в Supabase
4. Нажмите кнопку **"Run"** (или используйте Ctrl+Enter / Cmd+Enter)

### Шаг 4: Проверка успешного выполнения

После выполнения вы должны увидеть сообщение:
```
Success. No rows returned
```

Это значит триггер успешно создан! ✅

## 🧪 Как проверить, что работает?

### Тест 1: Удаление через Dashboard

1. Перейдите в **Table Editor** → таблица `posters`
2. Найдите любую запись
3. Запомните URL изображения из колонки `image_url`
4. Удалите эту запись (кнопка Delete)
5. Перейдите в **Storage** → bucket `posters`
6. Проверьте, что файл тоже удалился ✅

### Тест 2: Удаление через SQL

```sql
-- Удалите запись по ID (замените 123 на реальный ID)
DELETE FROM posters WHERE id = 123;
```

Файл должен удалиться автоматически!

## ⚠️ Важные замечания

### ✅ Преимущества
- **Автоматическое удаление** - не нужно помнить удалять файлы вручную
- **Работает всегда** - независимо от того, как вы удаляете (API, Dashboard, SQL)
- **Безопасность** - предотвращает накопление "мусорных" файлов в Storage
- **Экономия места** - Storage не будет засоряться неиспользуемыми файлами

### ⚠️ Что нужно знать
- Триггер срабатывает **ПЕРЕД** удалением записи
- Если файл уже удалён из Storage, ошибки не будет
- Триггер работает только при **DELETE**, не при UPDATE

## 🔧 Если что-то пошло не так

### Ошибка: "permission denied for schema storage"

**Решение:** Функция должна выполняться с правами `SECURITY DEFINER`, что уже сделано в коде.

### Ошибка: "function storage.delete does not exist"

**Решение:** Используйте другой синтаксис. Замените в SQL коде:

```sql
-- Вместо:
PERFORM storage.delete(
  bucket_id := 'posters',
  name := file_path
);

-- Используйте:
PERFORM storage.fdelete(ARRAY[
  ('posters', file_path)::storage.object_identifier
]);
```

## 📝 Дополнительная информация

### Просмотр созданных триггеров

Чтобы посмотреть все триггеры в базе данных:

```sql
SELECT
  trigger_name,
  event_manipulation,
  event_object_table
FROM information_schema.triggers
WHERE trigger_schema = 'public';
```

### Удаление триггера (если нужно)

```sql
DROP TRIGGER IF EXISTS delete_poster_image_trigger ON posters;
DROP FUNCTION IF EXISTS delete_poster_image();
```

## ✨ Готово!

Теперь при удалении постера файл будет удаляться автоматически!

Можете спокойно удалять записи из таблицы `posters` любым способом - файлы будут очищаться сами.
